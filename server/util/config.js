const config = {
  API_BASE: '/api',
  CONTACT_EMAIL: 'hellowordpedia@gmail.com',
  SCHEDULER_REPEAT_PATTERN: '0 8-18/3 * * 0-6' // at every 3rd hour from 8 through 18 on every day
};

module.exports = config;
